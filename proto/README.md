# Protocol Artifacts

This directory is reserved for protobuf sources and generated artifacts for the wire protocol.

If you add `.proto` files, keep them alongside any generated code or build instructions here.
